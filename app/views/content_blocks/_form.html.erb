<%= form_for(@content_block) do |f| %>
  <% if @content_block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_block.errors.count, "error") %> prohibited this content_block from being saved:</h2>

      <ul>
      <% @content_block.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>




<div id="asset_box">
	<p>
		<%= f.label :name, "Name" %>
		<br>
		<%= f.text_field :name, :class=>"text" %><br>
	</p>
	
	<p>
		<%= f.label :body, "Content" %>
		<br>
		<%= f.ckeditor_textarea("content_block", "body", :toolbar=>"Full", :width=>"600px", :height=>"350px")%>
	</p>

</div><!-- end asset_box-->
<p style="float:right;margin-top:20px"><input type="submit" name="some_name" value="" id="submit_btn"></p>

<%end%>
