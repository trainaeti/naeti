<h1>Listing shopping_carts</h1>

<table>
  <tr>
    <th>Course</th>
    <th>User</th>
    <th>Course date</th>
    <th>Quantity</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shopping_carts.each do |shopping_cart| %>
  <tr>
    <td><%= shopping_cart.course_id %></td>
    <td><%= shopping_cart.user_id %></td>
    <td><%= shopping_cart.course_date_id %></td>
    <td><%= shopping_cart.quantity %></td>
    <td><%= link_to 'Show', shopping_cart %></td>
    <td><%= link_to 'Edit', edit_shopping_cart_path(shopping_cart) %></td>
    <td><%= link_to 'Destroy', shopping_cart, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shopping cart', new_shopping_cart_path %>
